[project]
name = "codexhome"
version = "0.2.0"
description = "CodexHome repository memory and maintenance tooling"
requires-python = ">=3.13"
dependencies = [
    "keyring>=25.7.0",
    "pyrage>=1.3.0",
    "pyyaml>=6.0.3",
    "zstandard>=0.25.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.20",
]
